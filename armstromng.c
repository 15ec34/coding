#include <stdio.h>
void main()
{
int num,a =3,b=3;
printf("Print armstrong Numbers in a given range m to n:\n");
for (num = a; num <= b; num++)
{
if (num % 2 == 1)
 printf ("%d ", num);
 }
getch();
}
